# Docker

Store development and runtime container assets here.

Suggested files:

- base `Dockerfile`
- Jetson-specific `Dockerfile`
- `docker-compose.yml`
- image build notes

Provided files (from tutorial 1.3):

- `Dockerfile` — custom dev image based on `l4t-jetpack` (section 1.3.5)
- `docker-compose.yml` — Jupyter Lab container orchestration (section 1.3.9)
- `.dockerignore`

